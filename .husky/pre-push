#!/bin/sh

echo "🔎 Iniciando controles previos al push..."

CURRENT_BRANCH=$(git symbolic-ref --short HEAD 2>/dev/null)

if [ -z "$CURRENT_BRANCH" ]; then
  echo "⚠️  No se pudo determinar la rama actual."
  exit 1
fi

echo "🌿 Rama detectada: $CURRENT_BRANCH"

if [ "$CURRENT_BRANCH" = "main" ] || [ "$CURRENT_BRANCH" = "master" ]; then
  echo "⏭️  Saltando validaciones para rama principal."
  exit 0
fi

BLOCKED_WORDS="(wip|temp|test|tmp|draft|demo|asd|qwe|123|abc|backup|copy|new|old|sample)"

if echo "$CURRENT_BRANCH" | grep -E "$BLOCKED_WORDS" >/dev/null; then
  echo "❌ La rama contiene palabras no permitidas."
  echo "   ➜ Evitá nombres genéricos como: wip, temp, test, asd, 123, etc."
  exit 1
fi

VALID_PATTERNS="^(feature\/[a-z0-9-]+|fix\/[a-z0-9-]+|refactor\/[a-z0-9-]+|docs\/[a-z0-9-]+|chore\/[a-z0-9-]+|release\/[0-9]+\.[0-9]+\.[0-9]+|[A-Z]+-[0-9]+\/[a-z0-9-]+)$"

if echo "$CURRENT_BRANCH" | grep -E "$VALID_PATTERNS" >/dev/null; then
  echo "✅ Nombre de rama válido."
else
  echo ""
  echo "❌ Nombre de rama inválido: '$CURRENT_BRANCH'"
  echo ""
  echo "Usa uno de estos formatos:"
  echo "  • feature/nombre-descriptivo"
  echo "  • fix/arreglar-algo"
  echo "  • refactor/nombre"
  echo "  • docs/actualizar-docs"
  echo "  • chore/mantenimiento"
  echo "  • release/1.2.3"
  echo "  • ABC-123/descripcion"
  echo ""
  exit 1
fi

echo "🧪 Revisando typescript..."
npm run type-check >/dev/null 2>&1
if [ $? -ne 0 ]; then
  echo "❌ Falló la verificación de tipos."
  exit 1
fi

echo "🧹 Corriendo Biome..."
npx biome check --write src >/dev/null
if [ $? -ne 0 ]; then
  echo "❌ Biome detectó problemas que no pudo corregir."
  exit 1
fi

echo "🏗️ Compilando proyecto..."
npm run build
if [ $? -ne 0 ]; then
  echo "❌ Error en el build."
  exit 1
fi

echo ""
echo "🎉 Todo OK — Push autorizado!"
exit 0
